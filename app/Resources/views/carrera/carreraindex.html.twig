{% extends "layout/base-layout.html.twig" %}

{% block page_title %}Carreras{% endblock %}

{% block page_content %}

    <div class="box box-primary">
        <div style="width:90%; margin-left:5%; margin-top: 2% ">

            <div align="left">
                <a style="font-size: 15px" href="{{ path('carrera_new') }}" class="btn btn-success btn-sm"><i style="font-size: 20px" class="fa fa-plus-circle"></i> Carrera</a>
            </div>
            <br>

            <table class="table table-bordered table-hover table-striped table-responsive" style="width:100%" id="tabelMahasiswa">
                <thead class="thead-light">
            <tr>
                <th>Nombre</th>
                <th>Descripción</th>
                <th>Estado</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for carrera in carreras %}
            <tr>
                <td><a href="{{ path('carrera_show', { 'id': carrera.idcarrera }) }}">{{ carrera.nomcarrera }}</a></td>
                <td>{{ carrera.descripcioncarrera }}</td>
                <td>{% if carrera.estadocarrera %}Activo{% else %}Inactivo{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('carrera_show', { 'id': carrera.idcarrera }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('carrera_edit', { 'id': carrera.idcarrera }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
</div>
{% endblock %}
